apiVersion: batch/v1
kind: CronJob
metadata:
  name: sc-ingest-cronjob
  namespace: sc-ingest
spec:
  schedule: "*/15 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
          - name: sc-ingest-container
            image: vakintosh/sc-ingest:0.1.0 
            command: ["python3", "sc_ingest.py"] 
            env:
            - name: DB_PATH 
              value: "/app/data/soundcloud_state.db" 
            - name: FEEDS_CONFIG_PATH 
              value: "/app/config/feeds.txt" 
            resources:
              requests:
                cpu: "50m"
                memory: "128Mi"
              limits:
                cpu: "250m"
                memory: "256Mi"
            volumeMounts:
            - name: state-volume
              mountPath: /app/data
            - name: config-volume
              mountPath: /app/config
              readOnly: true
          volumes:
          - name: state-volume
            persistentVolumeClaim:
              claimName: soundcloud-state-pvc
          - name: config-volume
            configMap:
              name: soundcloud-feeds-config
              items:
              - key: feeds.txt
                path: feeds.txt